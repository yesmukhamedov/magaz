import React from "react";

const Path = ({path}) => {

    return (
        <>
            {/*{path.reduce((text, item)=>{*/}
            {/*    return `${text}'\\'${item}`*/}
            {/*}, '')}*/}
        </>
    )
}

export default Path;